# smartcrop-sharp@2.0.8 仍把 peer 釘在 sharp@^0.32，本專案用 sharp@^0.34（API 對 smartcrop
# 用到的 metadata/resize/extract 皆相容）。它只在 authoring 端（上傳、npm run ratios）用到，
# 不參與 nuxt generate build。legacy-peer-deps 讓 npm install / npm ci（CI 用）一致放行此 peer。
legacy-peer-deps=true
